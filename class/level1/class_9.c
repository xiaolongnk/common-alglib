#include <stdio.h>

#define MAX 100

int main()
{
    int total = 0;
    int start_pos = 0;
    int flag_arr[MAX];
    for(int i = 0 ; i < MAX ; ++i) flag_arr = [1];

    while(1) {
        printf("please input total people(<%d):",MAX);
        scanf("%d", &total);
        printf("please input start pos(<%d) :",total);
        scanf("%d", &start_pos);
    }

    return 0;
}
